# Copy to .env and fill in what you have. Everything is optional —
# without these the demo still runs (Webex posts are logged instead).

# Webex developer account (developer.webex.com) — real incident-room posts
WEBEX_TOKEN=
WEBEX_ROOM_ID=

# Optional cloud LLM triage upgrade (rules-based triage works without it)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
